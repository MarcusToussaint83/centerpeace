/**
 * @centerpeace/db
 *
 * Drizzle schema and migrations live here. Concrete schema arrives with
 * Milestone 0 (foundation) and grows through M1 (orgs/events/members).
 *
 * See ../../DATA_MODEL.md for the full schema spec.
 */

export const PLACEHOLDER = true;
